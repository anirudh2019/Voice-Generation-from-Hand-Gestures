# Voice-Generation-from-Hand-Gestures

To setup environment, create new conda environment and run the following command:
- pip install -r requirements.txt

That's it! Now run the following to run the app:
- python app.py
